export const updateParkPlaceRequestModel = (ParkPlaceID, VehicleCategory, PlaceCategory, Name, Longitude, Latitude, BasePrice, PriceTimeDuration) => ({
    ParkPlaceID,
    VehicleCategory,
    PlaceCategory,
    Name,
    Longitude,
    Latitude,
    BasePrice,
    PriceTimeDuration
})

export const updateParkPlaceResponseModel = (ParkPlaceID, VehicleCategory, PlaceCategory, Name, Longitude, Latitude, BasePrice, PriceTimeDuration) => ({
    ParkPlaceID,
    VehicleCategory,
    PlaceCategory,
    Name,
    Longitude,
    Latitude,
    BasePrice,
    PriceTimeDuration
})