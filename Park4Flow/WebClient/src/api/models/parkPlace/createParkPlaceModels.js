export const createParkPlaceRequestModel = (VehicleCategory, PlaceCategory, Name, Longitude, Latitude, BasePrice, PriceTimeDuration) => ({
    VehicleCategory,
    PlaceCategory,
    Name,
    Longitude,
    Latitude,
    BasePrice,
    PriceTimeDuration
})

export const createParkPlaceResponseModel = (VehicleCategory, PlaceCategory, Name, Longitude, Latitude, BasePrice, PriceTimeDuration) => ({
    VehicleCategory,
    PlaceCategory,
    Name,
    Longitude,
    Latitude,
    BasePrice,
    PriceTimeDuration
})