export const userEmailResponseModel = (UserID, Email, FirstName, SecondName, PhoneNumber, IsBanned, IsBusiness, Role) => ({
    UserID,
    Email,
    FirstName,
    SecondName,
    PhoneNumber,
    IsBanned,
    IsBusiness,
    Role
})