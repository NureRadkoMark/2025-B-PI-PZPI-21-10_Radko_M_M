export const getUserParkingListResponseModel = (ParkingID, Address, Name, Info, IsActive, Longitude, Latitude, DynamicPricing, DemandFactor, PhotoImage) => ({
    ParkingID,
    Address,
    Name,
    Info,
    IsActive,
    Longitude,
    Latitude,
    DynamicPricing,
    DemandFactor,
    PhotoImage
})