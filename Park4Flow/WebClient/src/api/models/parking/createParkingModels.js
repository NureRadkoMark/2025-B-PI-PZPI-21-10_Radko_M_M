export const createParkingRequestModel = (Address, Name, Info, Longitude, Latitude, DynamicPricing, DemandFactor, PhotoImage) => ({
    Address,
    Name,
    Info,
    Longitude,
    Latitude,
    DynamicPricing,
    DemandFactor,
    PhotoImage
})

export const createParkingResponseModel = (Address, Name, Info, Longitude, Latitude, DynamicPricing, DemandFactor, PhotoImage) => ({
    Address,
    Name,
    Info,
    Longitude,
    Latitude,
    DynamicPricing,
    DemandFactor,
    PhotoImage
})