export const updateParkingRequestModel = (Address, Name, Info, Longitude, Latitude, DynamicPricing, DemandFactor) => ({
    Address,
    Name,
    Info,
    Longitude,
    Latitude,
    DynamicPricing,
    DemandFactor
})

export const updateParkingResponseModel = (Address, Name, Info, Longitude, Latitude, DynamicPricing, DemandFactor, PhotoImage) => ({
    Address,
    Name,
    Info,
    Longitude,
    Latitude,
    DynamicPricing,
    DemandFactor,
    PhotoImage
})