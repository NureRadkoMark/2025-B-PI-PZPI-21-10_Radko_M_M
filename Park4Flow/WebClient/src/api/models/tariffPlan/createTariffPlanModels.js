export const createTariffPlanRequestModel = (SubscriptionDuration, SubscriptionPrice, Currency, Type) => ({
    SubscriptionDuration,
    SubscriptionPrice,
    Currency,
    Type
})

export const createTariffPlanResponseModel = (SubscriptionDuration, SubscriptionPrice, Currency, Type) => ({
    SubscriptionDuration,
    SubscriptionPrice,
    Currency,
    Type
})